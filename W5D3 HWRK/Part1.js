function madlibs(verb, adj, noun) {
  return "We shall" + verb.toUpperCase() + "the" + adj.toUpperCase() + noun.toUpperCase()
}

function isSubstring(searchString, subString) {
  searchString.include(subString)
}
